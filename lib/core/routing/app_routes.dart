/// Named route constants. Centralised so screens never hard-code route
/// strings inline and new routes (added feature-by-feature in later
/// blocks) have one obvious place to be registered.
abstract final class AppRoutes {
  static const String splash = '/';
  static const String home = '/home';
}
